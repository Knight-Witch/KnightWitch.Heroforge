{
  "schemaVersion": 1,
  "devBranch": "WITCH_DEV_MAIN",
  "stableBranch": "Witch_Scripts",
  "stableBaseline": "273b2dc2bbb7a38ea1591abf7c7723d23800e4a4",
  "runtimeDivergences": [
    {
      "issue": 19,
      "status": "canonical-main-protected",
      "reason": "Canonical Dev must have a distinct installed launcher/visible identity and route normal runtime to WITCH_DEV_MAIN rather than silently executing Stable while appearing to be Dev. Issue-scoped task branches may temporarily self-route for isolated validation, but must normalize before canonical integration.",
      "paths": ["Witch_Dock_DEV.user.js", "manifest.json"],
      "acceptance": [
        "Canonical Tampermonkey name and Dock title report the same DEV version identity.",
        "Canonical update/download URLs, manifest URL, and manifest-loaded runtime URLs resolve to WITCH_DEV_MAIN.",
        "A Dev bootstrap/channel-seam failure is visible rather than silently falling back to Stable."
      ]
    },
    {
      "issue": 10,
      "status": "task-branch-active",
      "reason": "Architecture work on wd/10-modular-bootstrap is incrementally moving runtime ownership out of the Stable-derived monolith. v1.3.2 externalizes only the compact emblem payload through an exact guarded source seam while preserving all other core behavior.",
      "paths": ["Witch_Dock_DEV.user.js", "manifest.json", "ASSETS/emblem.png"],
      "acceptance": [
        "Task launcher and manifest identify v1.3.2 / build 1.3.2-external-compact-emblem consistently.",
        "The launcher finds exactly one legacy inline compact-emblem declaration and fails visibly if that seam changes unexpectedly.",
        "Runtime Dev state reports presentationAssetMode external-compact-emblem and the task-branch ASSETS/emblem.png URL.",
        "#kwWDCompactIcon resolves to the task-branch ASSETS/emblem.png resource and remains visually unchanged.",
        "Module loader still completes 23/23 with zero failures and existing cache/order/enablement behavior is unchanged.",
        "Raw GM_* privileges remain unexposed page-side.",
        "No CSS, storage, registration, drag/minimize, hotkey, bone-HUD, or module-loader ownership moves in the v1.3.2 step.",
        "Before integration to WITCH_DEV_MAIN, task-specific URLs are normalized to canonical Dev and revalidated."
      ]
    }
  ],
  "notes": [
    "New Dev began directly from the Stable baseline above.",
    "Governance/documentation files are intentionally Dev-specific and are not runtime divergences.",
    "Every intentional runtime difference requires an open issue/task and this ledger.",
    "Task-branch self-routing is temporary validation infrastructure, not canonical channel behavior."
  ]
}
