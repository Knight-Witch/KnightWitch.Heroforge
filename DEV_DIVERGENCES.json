{
  "schemaVersion": 1,
  "devBranch": "WITCH_DEV_MAIN",
  "stableBranch": "Witch_Scripts",
  "stableBaseline": "95b5cdae4c8840d950d984c73bce101ba887011e",
  "runtimeDivergences": [
    {
      "issue": 19,
      "status": "canonical-dev-channel",
      "reason": "Canonical Dev must remain unmistakably Dev. The installed launcher updates from WITCH_DEV_MAIN and uses fixed Tampermonkey identity WITCH DOCK - DEV; each validated launcher revision may pin an immutable payload commit for runtime compatibility, with canonical WITCH_DEV_MAIN fallback routing and no Stable fallback.",
      "paths": [
        "Witch_Dock_DEV.user.js",
        "manifest.json"
      ],
      "acceptance": [
        "Tampermonkey @name remains fixed WITCH DOCK - DEV and @namespace remains KnightWitch.",
        "Launcher @version/runtime DEV_VERSION/manifest launcher version/build/visible Dock version stay synchronized.",
        "Launcher update/download URLs resolve to WITCH_DEV_MAIN.",
        "Normal runtime resolves manifest/core/modules from the launcher's immutable payload commit; fallback routing is WITCH_DEV_MAIN and never Stable.",
        "Bootstrap/channel failure remains visible."
      ]
    },
    {
      "issue": 20,
      "status": "dev-validated-awaiting-stable-promotion",
      "reason": "Booth JSON file I/O is implemented and passed automated + human real-file Dev validation; awaiting explicit narrow Stable promotion authorization.",
      "paths": [
        "tools/Booth.js",
        "manifest.json"
      ],
      "acceptance": [
        "Current Hero Forge Booth config round-trips through Save/Load without private React patching.",
        "Camera, background, lighting, effects, and overlays restore where present in the saved source.",
        "Legacy Lob effect-only JSON is accepted without pretending missing camera/background/lighting data exists.",
        "Invalid JSON fails closed and unrelated Booth/media behavior remains healthy."
      ]
    }
  ],
  "notes": [
    "Issue #28 is complete: modular v2.0.0 is live on Witch_Scripts and passed the actual Stable smoke.",
    "Promoted runtime/core/tool files are byte-identical between canonical Dev and Stable; only the intentional canonical Dev channel identity/routing remains divergent.",
    "Governance/documentation files are not runtime divergences.",
    "HF-Chat-Bridge is development infrastructure only and never a Witch Dock runtime dependency."
  ]
}
