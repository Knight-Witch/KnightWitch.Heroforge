{
  "schemaVersion": 1,
  "devBranch": "WITCH_DEV_MAIN",
  "stableBranch": "Witch_Scripts",
  "stableBaseline": "273b2dc2bbb7a38ea1591abf7c7723d23800e4a4",
  "runtimeDivergences": [
    {
      "issue": 19,
      "status": "canonical-main-protected",
      "reason": "Canonical Dev must have a distinct installed launcher/visible identity and route normal runtime to WITCH_DEV_MAIN rather than silently executing Stable while appearing to be Dev. Tampermonkey identity must remain stable across versions so updates replace the existing Dev install instead of creating duplicates. Issue-scoped task branches may temporarily self-route for isolated validation, but must normalize before canonical integration.",
      "paths": [
        "Witch_Dock_DEV.user.js",
        "manifest.json"
      ],
      "acceptance": [
        "Canonical Tampermonkey @name is the fixed WITCH DOCK - DEV identity and @namespace remains stable.",
        "Canonical userscript @version, runtime DEV_VERSION, manifest launcher version/build, and visible Dock title version remain synchronized.",
        "Canonical update/download URLs, manifest URL, and manifest-loaded runtime URLs resolve to WITCH_DEV_MAIN.",
        "A Dev bootstrap/channel-seam failure is visible rather than silently falling back to Stable."
      ]
    },
    {
      "issue": 10,
      "status": "task-branch-active",
      "reason": "Architecture work on wd/10-modular-bootstrap is moving runtime ownership out of the Stable-derived monolith. v1.3.8 + modal v0.1.1 pass automated modal lifecycle checks. Booth v27.0.6 + bootstrap v0.2.1 now call existing optional named media readiness seams on explicit state transitions/completion while preserving polling as fallback; the live manual-reload ON/OFF/ON gate passed with loader 23/23, one Booth script, zero duplicates, and 4K/8K/WebP following Booth state without manual readiness calls. Remaining gate is human visual confirmation of the About modal.",
      "paths": [
        "Witch_Dock_DEV.user.js",
        "manifest.json",
        "features/core/Witch_Dock_Styles.css",
        "features/core/Witch_Dock_Modals.js",
        "features/booth/Booth_Runtime_Bootstrap.js",
        "tools/Booth.js"
      ],
      "acceptance": [
        "witch-dock-styles remains v0.1.0 / build 0.1.0-extracted-core-css and the v1.3.6 CSS gate remains healthy.",
        "The modal module receives only script metadata plus the existing GitHub and Ko-fi URLs; raw GM_* privileges remain unexposed page-side.",
        "The launcher finds exactly one guarded legacy modal block containing each of the six legacy modal functions exactly once and replaces only those implementations with wrappers to KWWitchDockModals.",
        "About/Disclaimer preserve existing DOM ids/classes/content, mutual exclusion, close-button/overlay/Escape behavior, links, and version display; reopen creates no duplicate overlays.",
        "Module loader remains 23/23 with zero failures and existing cache/order/enablement behavior is unchanged.",
        "No storage, registration, drag/minimize, hotkey, undo/redo, bone-HUD, or unrelated feature lifecycle ownership moves in v1.3.7.",
        "Checked-in Witch_Dock.user.js remains Stable-derived during this guarded migration seam; physical source deletion waits for the later true core split.",
        "Before eventual integration to WITCH_DEV_MAIN, task-specific URLs are normalized to canonical Dev and revalidated.",
        "An explicit Booth session request invokes the optional bootstrap requestSession handoff immediately; the 200 ms poll remains fallback rather than the sole trigger.",
        "Direct handoff still loads at most one version-matched HeroForge /gated/booth.js and delegates activation to BT.setBoothMode without directly forcing maker.enable().",
        "Task launcher/manifest identify v1.3.8 / build 1.3.8-modal-lazy-about-fix; Tampermonkey @name remains fixed WITCH DOCK - DEV.",
        "witch-dock-modals is v0.1.1 / build 0.1.1-lazy-about-open; openAbout performs its own lazy ensureAbout creation on a fresh page.",
        "Fresh-page About/Disclaimer lifecycle and human visual parity must pass before Stage C continues.",
        "booth-tool is v27.0.6 / build v27.0.6-media-readiness-handoff and booth-runtime-bootstrap is v0.2.1 / build 0.2.1-media-readiness-handoff with synchronized deterministic cache keys.",
        "Booth session transitions and bootstrap completion use existing optional named media readiness seams so 4K/8K/WebP readiness does not depend solely on page timers; polling remains fallback.",
        "No media module source or HeroForge private runtime contract is changed by the readiness handoff.",
        "Manual-reload live gate passes: loader 23/23 / 0 failed; first Booth ON enables 4K/8K/WebP without manual readiness calls; OFF disables them; second ON reuses live BT with one Booth script / zero duplicates and re-enables them."
      ]
    }
  ],
  "notes": [
    "New Dev began directly from the Stable baseline above.",
    "Governance/documentation files are intentionally Dev-specific and are not runtime divergences.",
    "Every intentional runtime difference requires an open issue/task and this ledger.",
    "Task-branch self-routing is temporary validation infrastructure, not canonical channel behavior."
  ]
}
