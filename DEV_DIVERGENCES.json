{
  "schemaVersion": 1,
  "devBranch": "WITCH_DEV_MAIN",
  "stableBranch": "Witch_Scripts",
  "stableBaseline": "273b2dc2bbb7a38ea1591abf7c7723d23800e4a4",
  "runtimeDivergences": [
    {
      "issue": 19,
      "status": "canonical-main-protected",
      "reason": "Canonical Dev must have a distinct installed launcher/visible identity and route normal runtime to WITCH_DEV_MAIN rather than silently executing Stable while appearing to be Dev. Tampermonkey identity must remain stable across versions so updates replace the existing Dev install instead of creating duplicates. Issue-scoped task branches may temporarily self-route for isolated validation, but must normalize before canonical integration.",
      "paths": ["Witch_Dock_DEV.user.js", "manifest.json"],
      "acceptance": [
        "Canonical Tampermonkey @name is the fixed WITCH DOCK - DEV identity and @namespace remains stable.",
        "Canonical userscript @version, runtime DEV_VERSION, manifest launcher version/build, and visible Dock title version remain synchronized.",
        "Visible Dock title is formatted WITCH DOCK - DEV v<version>; the changing version is not embedded in Tampermonkey @name.",
        "Canonical update/download URLs, manifest URL, and manifest-loaded runtime URLs resolve to WITCH_DEV_MAIN.",
        "A Dev bootstrap/channel-seam failure is visible rather than silently falling back to Stable."
      ]
    },
    {
      "issue": 10,
      "status": "task-branch-active",
      "reason": "Architecture work on wd/10-modular-bootstrap is incrementally moving runtime ownership out of the Stable-derived monolith. The failed external compact-emblem experiment is rolled back. v1.3.4 restored the exact known-good inline emblem and passed the human visual gate; v1.3.5 keeps that source intact and enlarges only the icon inside the existing compact button using the bounded host style capability before dedicated CSS extraction resumes.",
      "paths": ["Witch_Dock_DEV.user.js", "manifest.json"],
      "acceptance": [
        "Task launcher and manifest identify v1.3.5 / build 1.3.5-larger-compact-emblem consistently.",
        "Tampermonkey @name remains fixed as WITCH DOCK - DEV while the visible Dock title is WITCH DOCK - DEV v1.3.5.",
        "The launcher finds exactly one legacy inline compact-emblem declaration and preserves it without substituting ASSETS/emblem.png.",
        "Runtime Dev state reports presentationAssetMode inline-core-emblem-restored, compactEmblemUrl inline:data-url-from-core, and compactIconSizePx 48.",
        "#kwWDCompactIcon computes to 48x48 while #kwWDCompact remains 54x54; the human visual gate confirms the emblem is still correct and appropriately sized.",
        "Module loader still completes 23/23 with zero failures and existing cache/order/enablement behavior is unchanged.",
        "Raw GM_* privileges remain unexposed page-side.",
        "No storage, registration, drag/minimize, hotkey, bone-HUD, module-loader, or feature lifecycle ownership moves in the v1.3.5 size adjustment.",
        "Before integration to WITCH_DEV_MAIN, task-specific URLs are normalized to canonical Dev and revalidated."
      ]
    }
  ],
  "notes": [
    "New Dev began directly from the Stable baseline above.",
    "Governance/documentation files are intentionally Dev-specific and are not runtime divergences.",
    "Every intentional runtime difference requires an open issue/task and this ledger.",
    "Task-branch self-routing is temporary validation infrastructure, not canonical channel behavior."
  ]
}
