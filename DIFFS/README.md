# Diffs

Optional storage for standalone patch files.

Normal updates should rely on GitHub commit history, `CHANGELOG.md`, `MASTER.md`, and `PRE_FLIGHT_Check.md`.

Use this folder only when a complex or risky change needs a preserved standalone patch outside normal GitHub history.
